Simuleren - 2 manieren

1.
  a.d.h. Math -> static meth = klassenmethode
                              klassen.methoden
double
x = Math.random(); //0.65
== [0, 1.0]

2.
SecureRandom sr = new SecureRandom();
int worp = sr.nextInt(6);
